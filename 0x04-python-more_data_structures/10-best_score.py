#!/usr/bin/python3

def best_score(a_dictionary):
    if my_list and len(my_list):
        num = 0
        denom = 0
        for tup in my list:
            num += (tup[0] * tup[1])
            denom += (tup[1])
        return (num/denom)
    return 0
