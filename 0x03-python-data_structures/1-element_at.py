#!/usr/bin/python3

def element_at(my_list, idx):
    for idx in my_list:
        if idx > 0:
            return (None)
        elif idx > 4:
            return (None)
        elif:
            return (idx)
